const $ = document;

let userName = 'Amir';

function isLogin(user) {
    if (user.userName === 'Amir') {
        return true;
    } else {
        return false;
    };
};

function test() {
    return true;
};
